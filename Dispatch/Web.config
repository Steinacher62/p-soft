<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.web>
    <!-- Impersonation needed to access word application -->
    <!--identity impersonate="true" /-->
    <compilation>
      <assemblies />
    </compilation>
  </system.web>
</configuration>